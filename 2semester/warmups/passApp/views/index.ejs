<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Passport App</title>
  </head>
  <body>
    <% if (!isAuthenticated) { %>
      <a href="/login">Log In</a>
    <% } else { %>
      Hello, <%= user.name %>!
      <% } %>
  </body>
</html>
